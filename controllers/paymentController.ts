import { Request, Response } from 'express';
import { ordenesPago, eventosPago } from '../data/payData';

export const obtenerOrdenPago = (Request: Request, response: Response): void => {
 
};

export const procesarWebhookPago = (Request: Request, response: Response): void => {

};